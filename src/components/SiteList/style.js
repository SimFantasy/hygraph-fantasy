import styled from 'styled-components'
import s from '@/styles/global.style'

export const SiteListWrap = styled.section`
  .site-list-container {
    ${s.gridCols(3, '24px')};
    width: 100%;
  }
`
