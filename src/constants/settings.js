// 站点名称
const siteName = 'Sim.z'

// 首页Author ID
const authorId = 'clcknahfd37my0azov7vkuydv'

// 首页 Feature Count ----

// Posts 数量 & 是否显示封面图片
const featurePostsCount = 3
const featurePostsCoverVisible = false

// Portfolios 数量
const featurePortfoliosCount = 3

// Snippets 数量
const featureSnippetsCount = 3

// Sites 数量
const featureSitesCount = 3

// 列表页 Count ----

// Posts 数量
const listPostsCount = 3

// Portfolios 数量
const listPortfoliosCount = 3

// Snippets 数量
const listSnippetsCount = 3

// Sites 数量
const listSitesCount = 3

export {
  siteName,
  authorId,
  featurePostsCount,
  featurePostsCoverVisible,
  featurePortfoliosCount,
  featureSnippetsCount,
  featureSitesCount,
  listPostsCount,
  listPortfoliosCount,
  listSnippetsCount,
  listSitesCount
}
