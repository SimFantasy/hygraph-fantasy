import styled from 'styled-components'
import s from '@/styles/global.style'

export const SnippetsWrap = styled.section`
  .snippets-container {
    padding-top: var(--sim-space-2xl);
    padding-bottom: var(--sim-space-4xl);
  }
`
